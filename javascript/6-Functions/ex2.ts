const multiple = (a : number, b : number): number => {
    return a * b;
}

function sum(number1:number, number2:number){
    return number1 + number2;
  }

  const sum2 = function(number1:number, number2:number){
    return number1 + number2;
 }


console.log(multiple(3,5));
console.log(multiple(7,3));